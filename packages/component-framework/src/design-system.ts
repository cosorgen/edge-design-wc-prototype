/**
 * Design system configuration for the component library.
 *
 * @public
 */
export const ComponentDesignSystem = Object.freeze({
    prefix: "mai",
    shadowRootMode: "open",
    registry: globalThis.customElements,
});
