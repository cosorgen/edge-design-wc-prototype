{
  "name": "edge-design-wc-prototype",
  "private": true,
  "version": "0.0.1",
  "description": "A web component based prototype for the Edge browser",
  "main": "src/index.js",
  "author": "",
  "license": "MIT",
  "workspaces": [
    "packages/utilities",
    "packages/component-framework",
    "packages/design-tokens",
    "packages/button",
    "packages/toggle-button",
    "packages/phoenix-theme",
    "packages/kumo-theme",
    "packages/fluent-theme",
    "packages/copilot-theme",
    "packages/windows-theme",
    "client",
    "server"
  ],
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "build": "node scripts/build.mjs",
    "build:pkg": "npm run build -ws --if-present",
    "clean": "rimraf dist",
    "clean:pkg": "npm run clean -ws --if-present",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "prettier": "prettier --check .",
    "prettier:fix": "prettier --write ."
  },
  "devDependencies": {
    "@eslint/js": "^9.15.0",
    "child_process": "^1.0.2",
    "esbuild": "^0.24.0",
    "eslint": "^9.15.0",
    "eslint-config-prettier": "^9.1.0",
    "open": "^10.1.0",
    "prettier": "^3.3.3",
    "typescript": "^5.6.3",
    "typescript-eslint": "^8.15.0"
  }
}
