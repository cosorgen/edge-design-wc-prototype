{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "noEmit": true // Don't emit JS files, just use typechecking.
  },
  "include": ["./index.ts", "./api/**/*"],
  "exclude": ["node_modules"]
}
