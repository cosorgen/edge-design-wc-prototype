import { html, css, customElement, FASTElement } from '@microsoft/fast-element';

export const template = html<EdgeNewtab>``;

export const styles = css``;

@customElement({
  name: 'edge-newtab',
  template,
  styles,
})
export class EdgeNewtab extends FASTElement {}
