{
  "name": "@edge-design-wc-prototype/client",
  "private": true,
  "type": "module",
  "version": "0.0.1",
  "description": "A web component based prototype for the Edge browser",
  "main": "src/index.js",
  "author": "",
  "license": "MIT",
  "scripts": {
    "start": "esbuild --bundle --minify --watch --outfile=www/bundle.js --servedir=www src/index.ts",
    "build": "esbuild --bundle --minify --outfile=www/bundle.js src/index.ts",
    "lint": "eslint src",
    "lint:fix": "eslint --fix src",
    "prettier": "prettier --write \"src/**/*.{js,ts,css,html,json}\""
  },
  "dependencies": {
    "@fluentui/web-components": "^3.0.0-beta.8",
    "@microsoft/fast-element": "^2.0.0-beta.26",
    "@phoenixui/themes": "^0.0.7"
  },
  "devDependencies": {
    "esbuild": "^0.19.0"
  },
  "imports": {
    "#services*": "./src/services/*"
  }
}
